---
project: gestor-comercial-python
domain: PDV desktop offline (food truck do pai do Vitor)
status: Remasterização da V1 CONCLUÍDA em 2026-09-06 — as 8 fases (0 a 7) fechadas, suíte 800 verdes, 0 xfail, 0 falhas. Onde paramos e o porquê: §0 do REMASTERIZACAO-V1.md. A Fase 7 comparou o sistema com o código de ANTES da faxina (b22da75) pelos dois produtos que o pai do Vitor vê: 11 telas em 24 estados (2 temas + filtro por operador) e os 6 cupons ESC/POS. Cupons idênticos linha a linha; das 24 telas, 9 idênticas byte a byte e 15 diferentes — 13 delas são os defeitos que a faxina matou (cartão fantasma, célula empilhada, cor congelada no tema claro), agora visíveis lado a lado. A comparação achou UMA regressão da própria remasterização, corrigida com teste: a seção "Cópia de Segurança" (Fase 2) empurrou Configurações além da altura da página e o layout espremeu os botões de Senhas e Acesso até ficarem sem rótulo num monitor de 768px. Bancadas versionadas: tools/comparar_telas.py e tools/comparar_cupons.py. Depois disso, em 2026-09-06, o item de layout que a Fase 7 deixou registrado foi feito (§9 do REMASTERIZACAO-V1.md): o cartão "Recebimentos" da tela de Caixa cortava as linhas ao meio a 1366x738 — defeito anterior à faxina, igual em b22da75 — e ganhou a mesma QScrollArea da Configurações na coluna de resumo, com a largura da barra reservada para não trocar corte de cima por corte de lado. Suíte 801, teste próprio, e as 24 renderizações conferidas nos dois tamanhos: 22 idênticas byte a byte, diferindo só a tela corrigida. A bancada tools/comparar_telas.py ganhou --tamanho LxA. Depois disso, em 2026-09-08, o modal "Adicionar item" (a tela por onde entra cada produto de cada comanda) foi refeito por pedido do Vitor, a partir de dois mockups — §9.4 do REMASTERIZACAO-V1.md. A janela legada dentro de comanda_view.py (QFormLayout + QSpinBox + lista de texto corrido) virou src/gestor_comercial/ui/widgets/adicionar_item_dialog.py: cartão sem moldura com escurecedor, busca com anel âmbar no foco, pílulas de filtro por categoria, lista com miniatura circular do produto (thumbnail_cache ganhou o formato CIRCULO), stepper de quantidade, observação e prévia de preço × quantidade. Regra de negócio nenhuma mudou, e o fluxo rápido continua: Enter lança e o modal fica aberto para o próximo item. O ganho medido está na digitação: lancar_item faz commit, o commit expira as instâncias do SQLAlchemy, e por isso a busca antiga voltava a bater no banco a cada tecla DEPOIS de cada item lançado — 120 consultas em 3 teclas sobre o cardápio real de 113 produtos. Agora são 0 em qualquer estado, ao custo de 128 consultas uma vez na abertura (o N+1 do §3.6; viraria 2 com selectinload no produto_repository, deixado fora de propósito). A renderização com o cardápio real pegou um defeito que a suíte não pegava: com 15 categorias a faixa de pílulas transbordava por cima da lista — corrigido com teto de 2 fileiras e rolagem, e trancado em teste. As peças comuns dos modais em cartão saíram para ui/widgets/cartao_modal.py (escurecedor, preparar_botao, centralizar_no_pai), compartilhadas com o pin_pad_dialog. Suíte 946 (de 918), 28 testes novos. O BuscaProdutoWidget antigo continua de pé porque o modal "Adicionar componente" do Cardápio ainda o usa. Ainda em 2026-09-08, dois pedidos do Vitor no mesmo dia — §9.5 do REMASTERIZACAO-V1.md. (1) O modal "Novo funcionário" saiu de dentro de funcionarios_view.py (QFormLayout + QComboBox, moldura do sistema) para src/gestor_comercial/ui/widgets/funcionario_dialog.py, terceiro modal em cartão do app: badge de identidade com ícone desenhado, avatar que monta as iniciais enquanto se digita, os cinco cargos como cards com descrição em grade de 2 colunas, telefone com máscara, pílulas Ativo/Inativo e um rodapé que diz o que o cargo libera (CAIXA · ACESSO LIBERADO). Regra de negócio nenhuma mudou e método de service nenhum é novo: o diálogo devolve DadosFuncionario e a view chama criar/editar; a situação usa ativar()/desativar(), que já existiam. A derivação de ACESSO, que estava solta na view, virou CARGOS — o rodapé do modal e a linha ACESSO do painel de detalhe pararam de poder divergir. O quinto cargo do mockup é Entregador, não Atendente: CargoFuncionario.ATENDENTE virou ENTREGADOR, e como o valor do enum É o dado gravado na coluna, veio junto a migração a7f3c2e5d918 (sem ela um cadastro antigo perderia o cargo, calado, na primeira edição). (2) Mesa ocupada deixou de ser ciano e virou Vermelho Ferrari #DC2626, o mesmo nos dois temas, com o corpo do card tingido (#2D1214 no escuro, #FEE2E2 no claro) — borda, palavra OCUPADA, número, valor e os dois pontinhos de legenda (resumo do salão e painel de comandas) acompanham. Dois seletores que nada tinham a ver com mesa (badge de despesa do Caixa e link de pendências das Impressoras) pegavam aquele ciano emprestado e foram separados para ciano_metrica, com varredura de teste que reprova quem recolar o empréstimo. O fundo tingido revelou um corte anterior à cor: o card ocupado tem 4 linhas, media 110px e era desenhado com 96 — o valor em aberto saía cortado ao meio e o atendente não aparecia. O piso subiu para ALTURA_PX = 112, com teste do invariável (nenhum card menor que o próprio sizeHint); esse teste só consegue falhar porque a fixture registra a fonte da marca — a plataforma offscreen sobe sem banco de fontes e ali o aperto não acontece. Suíte 1009 (de 946), 63 testes novos; bancada visual com 22 das 24 telas idênticas byte a byte, diferindo só as duas de Mesas. Duas coisas ficaram registradas e fora deste item: as 8 colunas fixas da grade de mesas não cabem na largura útil de 1366px (anterior, independente da cor), e as iniciais do avatar seguem sendo primeira+última palavra, e não as duas primeiras do mockup. Ainda em 2026-09-08, o terceiro pedido do dia — §9.6 do REMASTERIZACAO-V1.md: a mini-tela de sangria/reforço/despesa (`_MovimentoDialog`, dois QLineEdit num QFormLayout dentro de caixa_view.py) virou src/gestor_comercial/ui/widgets/movimentacao_caixa_dialog.py, quarto modal em cartão do app, com teclado numérico na própria tela. É UMA classe parametrizada por TipoMovimento, não três: `OPERACOES` guarda numa linha o que separa uma operação da outra (ícone, título, subtítulo, sugestões de descrição, rótulo do botão e os nomes dos tokens de cor), e o `papel` dela chega a exatamente dois widgets — o badge do cabeçalho e o botão que grava. O visor conta CENTAVOS inteiros entrando pela direita, como máquina de cartão (5,0,0,0 → R$ 50,00): isso apaga a classe inteira de defeito do "valor ilegível", porque nunca há texto para ler de volta, e o teto sai de dinheiro.LIMITE para o numpad não conseguir montar um valor que registrar_movimento recusaria com ValueError (que não está em _ERROS_SERVICE e subiria como estouro). Regra financeira nenhuma mudou e método de service nenhum é novo: o diálogo devolve DadosMovimento e a view chama registrar_movimento(tipo, valor, descricao) igual a antes; quem exige gerente e quem recusa zero continua sendo o CaixaService. A tela de Caixa tinha TRÊS dicionários paralelos com as mesmas três chaves (rótulo, badge, variante do botão) e os três passaram a ler OPERACOES. Tokens `pin_tecla_*` viraram `tecla_numerica_*` (segundo numpad do app, mesmo critério do `botao_circular_*`), e cada operação ganhou família de cor própria em vez de pegar `perigo`/`sucesso` emprestados — sangria não é erro, reforço não é sucesso. `done()` NÃO zera o valor (o oposto do modal de PIN, e de propósito: `resultado()` é lido depois do `exec()`). Suíte 1085 (de 1025), 60 testes novos, conferidos com 8 mutações que reprovam. Renderização nativa: 460x611 (sangria/reforço) e 460x644 (despesa), sob os 728px úteis de um monitor de 768px. Ficaram de fora naquele dia: a abertura e o fechamento do caixa, feitos no dia seguinte. Ainda no mesmo tema, em 2026-09-09, o quarto pedido do Vitor sobre modais — §9.7 do REMASTERIZACAO-V1.md: as duas mini-telas que abrem e encerram o turno (`_ValorDialog` e `_FecharCaixaDialog`, 60 linhas dentro de caixa_view.py, com moldura do sistema e campos de texto) viraram cartões split-screen com numpad, o quinto e o sexto modais em cartão do app — src/gestor_comercial/ui/widgets/abertura_caixa_dialog.py e fechamento_caixa_dialog.py. Como seriam a terceira e a quarta cópia do acumulador de centavos, ele e o teclado saíram para widgets/teclado_numerico.py e a moldura comum para widgets/cartao_de_turno.py, com o modal do §9.6 MIGRADO junto: hoje há uma implementação só do numpad de dinheiro (o pin_pad ficou de fora de propósito — teclado sem 00, com ENTRAR no lugar do apagar). Diferente do §9.6, aqui é base + duas subclasses e não uma classe parametrizada: as colunas da esquerda não têm nada em comum, e parametrizar produziria a classe com metade dos atributos None conforme o modo. Regra financeira nenhuma mudou e método de service nenhum é novo: os diálogos devolvem DadosAbertura/DadosFechamento e a view chama abrir/fechar com a mesma assinatura; gerente, valor negativo, turno pendente e comanda em aberto continuam sendo barrados pelo CaixaService, e o comprovante continua saindo depois do fechamento pelo disjuntor de impressão. O ganho medido: com o numpad não há texto para ler de volta, então o `_ERRO_VALOR` ("Valor inválido...") SAIU da caixa_view — era este o último caminho que o justificava, como o §9.6 previu. No fechamento a diferença passou a ser mostrada enquanto se digita (falta em vermelho, exato em verde, sobra em ciano) e o teclado tem um destino por vez, dito em voz alta (DIGITANDO DINHEIRO / DIGITANDO MAQUININHAS) — antes o operador confirmava às cegas e descobria a quebra no papel. A prévia da diferença não pode chamar resumo() (nada foi gravado ainda), então a igualdade com ResumoCaixa.diferenca_total é trancada por teste que fecha o caixa de verdade. A única mudança de dado é a observação da abertura, que o mockup pede: coluna Caixa.observacao_abertura + migração d9b4c7e21f30 + parâmetro opcional em abrir() + a linha "Obs. abertura:" no cupom de fechamento — campo que o operador preenche e o sistema descarta seria pior que campo nenhum. Suíte 1179 (de 1086), 93 testes novos, conferidos com 12 mutações (11 reprovam; a 12ª, tecla a 120px, não reprova porque o cartão ainda cabe — a 200px reprova). Bancada visual: as 24 renderizações IDÊNTICAS byte a byte ao código anterior. Renderização nativa: 640x441 (abertura) e 640x520 (fechamento), sob os 728px úteis. De quebra, o teste de queda de energia (tests/integration) que piscava vermelho com a suíte maior foi corrigido: a retentativa de reabrir o banco envolvia só o sqlite3.connect, que é preguiçoso, e o "disk I/O error" aparecia no primeiro execute, fora do laço. Ficaram de fora: o _QuitarConsumoDialog, o "Adicionar componente" do Cardápio e o fluxo de senha do fechamento cego. Ainda em 2026-09-09, e mudando de assunto (não é mais modal), o pedido de SUB-MODELO — §9.8 do REMASTERIZACAO-V1.md: o cardápio real tem 15 categorias e 113 produtos, e dentro de "Lanches" convivem artesanal, podrão e cachorro quente. A categoria não pode ser quebrada em três porque é ela que decide a impressora, então o produto ganhou uma segunda etiqueta PURAMENTE ORGANIZACIONAL dentro dela: coluna produtos.subcategoria TEXT NULL String(80) + migração e2c7b4f9a613 + índice composto idx_produtos_categoria_sub (categoria_id, subcategoria), que o EXPLAIN QUERY PLAN confirma ser COVERING — a consulta que monta sugestões e pílulas responde sem abrir linha de produto. A REGRA DE OURO (roteamento 100% amarrado à categoria, nenhuma impressora reconfigurada) está trancada em três alturas: um teste que manda três lanches da mesma categoria com sub-modelos diferentes saírem NUM CUPOM SÓ, uma varredura que reprova a palavra "subcategoria" dentro do impressao_service (mesmo mecanismo do ciano emprestado do §9.5) e um teste de migração que confere categorias.impressora_id intacto; fora da suíte, tools/comparar_cupons.py deu os 2 cupons IDÊNTICOS linha a linha. Não é tabela com FK de propósito: sub-modelo não tem impressora, status nem regra — uma FK custaria CRUD, tela e órfãos para guardar só um nome. O preço de ser texto livre é a divergência de grafia, e quem paga é o service: _subcategoria_canonica compara ignorando acento/caixa/espaço e grava A GRAFIA QUE A CATEGORIA JÁ USAVA, então "podrao" numa categoria que já tem "Podrão" NÃO cria um segundo grupo (pílula de sugestão sozinha só cobre quem clica). A normalização saiu para services/texto.py porque a busca precisa da mesma regra — duas cópias divergindo fariam o mesmo sub-modelo agrupar numa tela e separar na outra. Nas telas: o cadastro de produto ganhou campo "Sub-modelo" opcional + pílulas de sugestão daquela categoria (clicar preenche, clicar de novo limpa, e a pílula acende junto com o campo, inclusive para quem digita à mão); a lista do Cardápio ganhou selo ao lado do nome + faixa de filtro (TODOS / cada sub-modelo / SEM SUB-MODELO); o modal "Adicionar item" mostra LANCHES · PODRÃO nos metadados e a busca acha por sub-modelo ("artesanal" traz os lanches desse grupo sem a palavra estar no nome de nenhum) — sem pílula de sub-modelo lá, porque o filtro de lá é por categoria, que é o eixo da impressora. TUDO some quando não há sub-modelo cadastrado, e é por isso que as 24 telas da bancada seguem IDÊNTICAS byte a byte: a funcionalidade é invisível até o Vitor classificar o cardápio na mão (o seed.py NÃO classifica nada — ele pula produto que já existe pelo nome, então classificar lá deixaria a máquina do Vitor e uma máquina limpa com cardápios diferentes). A renderização com dado real pegou um defeito que a suíte não pegava, de novo: com o sub-modelo "Cachorro Quente" o selo crescia até o QLabel do nome, que corta SEM reticências, e "Cachorro Quente Linguiça" virava "Cachorro Quente Lin". Corrigir custou duas armadilhas de Qt, ambas registradas — QFontMetrics.elidedText IGNORA letterSpacing (devolveu 88px para um teto de 76px, e o selo saía com a PRIMEIRA letra cortada), e fonte de QSS vence setFont (medir com QFont() media Segoe UI, mas quem pinta é a Archivo Black da marca, ~20% mais larga a 9px; a solução foi ensurePolished() e medir a fonte do próprio rótulo, sem gêmeo para divergir). O teste disso precisou de DUAS asserções, porque cortar demais também cabe no teto, e da fonte da marca registrada na fixture — sem ela o offscreen mede outra coisa e o teste passaria verde sem ter olhado. De quebra, _ProdutoDialog.resultado() virou a dataclass DadosProduto: a tupla iria a sete posições desempacotadas por ordem, e descricao/subcategoria são ambas str|None — trocar as duas gravaria a descrição no lugar do sub-modelo, calado. Suíte 1241 (de 1179), 62 testes novos, conferidos com 18 mutações (as 18 reprovam). Boot de ponta a ponta em banco novo (migrations + seed duas vezes): 60 mesas, 15 categorias, 113 produtos, 4 combos, WAL ligado. Ficaram de fora: sub-modelo no cupom da cozinha (a bobina é para quem produz, não para quem organiza — trancado em teste) e relatório por sub-modelo (o dado já está gravado e indexado para quando for). Ainda em 2026-09-09, o Vitor olhou a tela pronta e apontou que ela estava DE CABEÇA PARA BAIXO — §9.9 do REMASTERIZACAO-V1.md: "a subcategoria não deve ser dentro de um item e sim o inverso, os itens devem ser dentro de tal subcategoria, assim ao abrir uma categoria vemos suas subdivisões", e logo depois "temos que ter a tela com o design bonito para criar as subclasses". A segunda frase é a que mudou o MODELO: com a coluna de texto do §9.8 uma subcategoria só existia enquanto algum produto carregasse a string, então não havia como criar uma vazia esperando os itens — que é como alguém organiza um cardápio (primeiro os grupos, depois a classificação) — e renomear exigiria reescrever N produtos, uma reescrita em massa que falhando no meio deixa metade do cardápio num grupo e metade no outro. Virou entidade: tabela subcategorias (id, nome, categoria_id, par único por categoria) + produtos.subcategoria_id no lugar do texto + migração f8d1a6c40b27, que CONVERTE o dado existente antes de derrubar a coluna antiga (cada par categoria+texto vira uma linha, e o vínculo casa pelo par INTEIRO — sem isso um "Podrão" de Lanches poderia apontar para o "Podrão" de Porções e o produto trocaria de grupo sozinho). A canonização do §9.8 (gravar "Podrão" quando se digita "podrao") virou RECUSA com mensagem: com tela de cadastro, criar às escondidas uma coisa com nome diferente do digitado é pior que dizer "já existe". Subcategoria não tem `ativo` (categoria desativada some do balcão, que é regra de venda; subcategoria é só organização) e excluí-la NÃO apaga os produtos — eles voltam para "Sem subcategoria" via ondelete=SET NULL, porque excluir uma etiqueta nunca pode apagar o que ela etiquetava; excluir a CATEGORIA, essa sim, leva as subdivisões junto por cascade (subcategoria órfã não é alcançável por tela nenhuma). A REGRA DE OURO do §9.8 segue trancada nas mesmas três alturas, agora com subcategoria_id, e a bancada de cupons deu os 2 IDÊNTICOS linha a linha contra o commit anterior. A tela virou a hierarquia: a ÁRVORE da esquerda expande a categoria e mostra Todas / cada subdivisão / Sem subcategoria com a contagem de cada uma (uma categoria aberta por vez — quinze expandidas viram uma coluna de sessenta linhas), e a TABELA da direita agrupa por subdivisão com uma linha de cabeçalho por grupo (GUARNIÇÕES · 3 ITENS); escolher uma subdivisão mostra só ela e SEM cabeçalho, que seria um só dizendo o que o título já diz. Saíram: o selo da subcategoria na linha do produto (repetia por item o que o cabeçalho diz uma vez, e disputava largura com o nome — junto com ele foi embora todo o encurtamento do §9.8), a coluna "Tipo" (90px para uma badge que aparece em 4 de 113 produtos; COMBO virou selo ao lado do nome) e o KPI "Preço médio" (média sobre um cardápio de R$ 0,50 a R$ 48,00 não decide nada), que deu lugar a SUBCATEGORIAS. O modal "Nova subcategoria" (widgets/subcategoria_dialog.py) é o SÉTIMO modal em cartão do app: cartão de contexto dizendo a categoria e a IMPRESSORA dela (a regra de ouro dita em voz alta), campo com anel de foco e contador, as que já existem como pílulas apagadas e NÃO clicáveis (informam; clicar só poderia levar a "já existe"), e a conferência do nome rodando na tecla — o botão desliga antes de salvar. O pedido "não deixa nada ficar cortado" tinha uma causa anterior à subcategoria: setItemWidget numa QListWidget NÃO dimensiona o item, e a linha de duas alturas (nome + subtítulo) vinha sendo desenhada na altura de uma, cortando as duas ao meio em TODAS as quinze categorias. Mais três cortes apareceram na renderização com o cardápio real, os três trancados em teste: stretchLastSection nasce LIGADO no Qt e a coluna da contagem tomava 146 de 293px; setFirstColumnSpanned num item ainda SOLTO não vale (o span mora no modelo, e o item só entra nele no addTopLevelItem); e dois botões numa linha de 300px cortavam o próprio rótulo. De quebra, uma regra do Qt que vale registrar: setStyleSheet("background: transparent") num widget vale para os DESCENDENTES e vence o QSS global — os badges ATIVO/VAZIO e a barra de margem sumiram no meio do trabalho por isso, e a saída foi trocar o setStyleSheet do embrulho por um objectName. E o ganho que o §9.4 tinha deixado registrado finalmente entrou: listar_produtos_para_lancamento() com selectinload nas duas relações derrubou a abertura do modal de lançamento de 128 consultas para 3 — a subcategoria como relação teria DOBRADO a conta em vez de zerá-la. Suíte 1284 (de 1225), 59 testes novos, conferidos com 23 mutações (as 23 reprovam). Bancada visual: 22 das 24 telas idênticas byte a byte, diferindo só as duas do Cardápio, que é a tela reestruturada. Ficaram de fora: mover subcategoria de categoria (chegou a existir no rascunho e saiu — mudaria a impressora dos produtos, o que é correto mas ninguém pediu), subcategoria no cupom, relatório por subcategoria, e a barra lateral do mockup (Visão Geral, Delivery, Estoque, Fechamento são telas que não existem; o pedido era sobre o Cardápio). Ainda em 2026-09-09, o Vitor relatou um DEFEITO e pediu duas barreiras de credencial — §9.10 do REMASTERIZACAO-V1.md. (1) O defeito: excluir um operador/turno em Funcionários não persistia — no boot seguinte ele estava de volta. A exclusão sempre funcionou; as causas eram outras duas. A primeira é que o run_seed() roda a CADA abertura do programa e cada seed_* é idempotente por nome — e idempotente é o mesmo que RESTAURADOR: o registro apagado deixa de existir, o seed do boot seguinte conclui que "falta" e o cria de novo. Não era só dos turnos: produto e categoria excluídos no Cardápio voltavam pelo mesmo caminho, e por isso a correção é do run_seed() INTEIRO — tabela nova preferencias (chave→valor) com a marca bootstrap_concluido, gravada no MESMO commit do povoamento, e a migração a4c9f1d70b52 marcando o banco que já existe (senão a primeira abertura ressuscitaria, uma última vez, o que ele apagou). O sinal de "já povoado" é mesas+produtos e NÃO usuarios: as migrações d3f8a1c4e6b9/f4b2c8e1a7d5/d23a4f888a77 inserem Usuario por conta própria, e a f4b2c8e1a7d5 cria os dois turnos em TODO banco, inclusive num recém-criado — usá-los marcaria o bootstrap antes de ele acontecer e a instalação nasceria sem as 60 mesas e sem o cardápio. A segunda causa é que o Usuario de login nunca era excluído junto (tabelas separadas, §3.11), então o nome continuava no dropdown: FuncionarioService.excluir passou a aposentar o login de mesmo nome (par casado por nome, mesma convenção de listar_operadores_caixa), DESATIVANDO e nunca apagando, porque usuarios.id é FK de caixas/comandas/movimentos e apagar arrancaria o nome de toda venda daquele operador; a trava do último gerente é consultada ANTES de qualquer escrita (a regra saiu para AuthService.motivo_para_nao_desativar, que devolve a mensagem em vez de levantar). E o dropdown do login passou a abrir no último operador que ENTROU (preferencias.ultimo_operador_id, gravado depois de a autenticação passar), guardando só o id — quem confere se ele ainda está ativo é a tela; de quebra o userData virou id em vez da instância do SQLAlchemy (que expira a cada commit) e o showEvent recarrega a lista, senão o operador desativado continuaria sendo oferecido depois do logout. (2) Excluir funcionário passou a exigir a SENHA MASTER digitada na hora (PinPadDialog.para_exclusao, Nível 3, sem herança de baixo pra cima): exigir_gerente() era satisfeito pela SESSÃO, e quem abriu o turno de manhã autorizava qualquer exclusão clicada à tarde. (3) "Senhas e Acesso" ganhou um OLHO por linha que mostra o valor real por 8s mediante o CPF do Dono — e isto CONTRADIZ a regra escrita do §3.13 ("ver o valor não é uma operação que existe"): hash é via de mão única, então entrou uma segunda cópia recuperável (colunas loja_config.*_cifrada, migração b6e2d80a3f14, cifra de fluxo HMAC-SHA256 + selo em services/segredo_reversivel.py, sem dependência nova). O que ela protege está escrito lá sem rodeios: leitura casual do .db, NÃO quem tem o arquivo E o programa — a chave mora no mesmo banco porque o backup é uma cópia do .db, é ofuscação em repouso, e a barreira de verdade é o CPF na tela. Autenticação nenhuma mudou: senha_*_hash continua sendo a única coisa contra a qual um PIN é conferido, e revelar() só devolve texto. O backfill confere o hash em vez de chutar (técnica do c1d5b8e37a42): senha ainda de fábrica vira cópia, senha já trocada fica NULL e a tela diz "altere-a uma vez para poder visualizá-la". O CpfDonoDialog é o OITAVO modal em cartão (máscara •••.•••.•••-••, TecladoNumerico compartilhado, botão só liga com os 11 dígitos), e o valor revelado sai da tela por quatro caminhos no mesmo ocultar_revelado(): timer, segundo clique, troca do segredo e hideEvent (sem este último a senha ficaria acesa atrás de outra página). O ícone é DESENHADO e não o emoji 👁 (mesma armadilha do cadeado do PIN: cai no Segoe UI Emoji, sai chapado e ignora o tema), e a seção virou QGridLayout porque "Cadastrar" é mais largo que "Alterar" e desalinhava o olho daquela linha. Suíte 1418 (de 1290), 128 testes novos, conferidos com 24 mutações (as 24 reprovam); bancada visual com 22 das 24 telas idênticas byte a byte (diferindo só as duas de Configurações) e os 2 cupons idênticos; boot de ponta a ponta nos dois cenários reais (banco novo e banco que já rodava, com a exclusão sobrevivendo a duas reaberturas). Ficaram de fora: PIN na exclusão de produto/categoria do Cardápio (não foi pedido), registro de auditoria de quem olhou um segredo (não existe tabela de log no projeto) e o _AlterarSegredoDialog, que segue sendo o formulário antigo. Em 2026-09-11, o pedido do Cardápio em cartões — §9.11 do REMASTERIZACAO-V1.md: a tela do §9.9 (árvore com um widget por categoria + tabela com cinco células-widget por produto) virou a do mockup do Vitor, com as DUAS listas pintadas por QStyledItemDelegate e nenhum widget por linha (src/gestor_comercial/ui/widgets/cardapio_cartoes.py): KPIs elevados com insígnia âmbar desenhada (Categorias, Subcategorias, Produtos e Preço médio com a margem de legenda — o preço médio que o §9.9 tirou VOLTOU por ser o card do mockup), árvore com a categoria aberta em cartão, guia vertical e a subdivisão escolhida em pílula no acento, e à direita um bloco por subcategoria ("Guarnições · 3 PRODUTOS · Editar subcategoria") com miniatura, preço, custo e barra de margem. Cada linha carrega um instantâneo imutável, porque o commit expira as instâncias do SQLAlchemy e pintar o Produto seria consulta a cada repintura — trancado por teste que conta ZERO SQL numa repintura pós-commit. Medido no cardápio real: RSS +37,7 MB → +0,7 MB em 60 trocas de categoria e 20 recargas (não era vazamento, era o custo de criar e polir ~58 widgets contra um QSS de 94 KB a cada troca), troca de categoria 34 → 8 ms, recarga 41 → 12-15 ms e 39 → 10 consultas. A seleção volta pelo ID do produto (não pela linha), e salvar não fecha o acordeão nem rola nenhuma das duas listas. Regra de negócio saiu da view: margem_percentual e CardapioService.resumo_do_cardapio (3 consultas fixas, eram 17) e listar_todas_as_subcategorias (o N+1 da árvore, eram 15 por recarga). Roteamento de impressão intocado: os 2 cupons idênticos linha a linha. Achados no caminho, todos com teste: renomear a subcategoria escolhida jogava o gerente para a primeira categoria; o QColor NÃO entende rgba() do CSS (metade da paleta Concreto) e o placeholder das miniaturas pintava borda preta no escuro — ui/theme/cores.py::cor_do_token; a busca da árvore casava por "Todas"/"Sem subcategoria"; duplo clique fechava a categoria que o clique acabara de abrir. O PainelPontilhado passou a desenhar só a região suja (login e sidebar idênticos byte a byte). Ficaram no rodapé Editar, Desativar (fora do mockup, de propósito: é o gesto de todo dia) e Excluir em Vermelho Ferrari #DC2626. Saíram as pílulas de filtro e a coluna Status. Suíte 1495 (de 1426), 0 falhas; 24 mutações, 23 reprovam (a sobrevivente, um doItemsLayout defensivo, está medida e declarada); bancada com 20 das 24 telas idênticas byte a byte nos dois tamanhos. O teto de ~90 MB pedido NÃO é alcançável pela tela sozinha — o processo já mede ~100 MB antes do Cardápio existir — e está escrito assim no §9.11. Em 2026-09-12, o pedido dos dois mockups de cadastro do Cardápio, com "DRY" escrito acima do desenho — §9.12 do REMASTERIZACAO-V1.md: as janelas de criar CATEGORIA e criar SUBCATEGORIA viraram UM cartão só, src/gestor_comercial/ui/widgets/organizacao_cardapio_dialog.py, parametrizado por NivelDoCardapio. Antes eram duas telas para a mesma pergunta ("que nome tem este grupo?"): o `_CategoriaDialog` de 20 linhas dentro de cardapio_view.py (QFormLayout + QDialogButtonBox, a última janela do Cardápio que parecia formulário de 2005, sem contador, sem aviso de nome repetido) e o SubcategoriaDialog do §9.9, já em cartão mas anterior ao mockup e com uma cópia inteira de cabeçalho, campo, contador, rodapé e ciclo de vida. O que separa um nível do outro mora em PAPEIS, uma linha por papel (glifo, rótulo do campo, placeholder, destino padrão, regra de duplicidade e as seis frases de cada modo) — é a forma do §9.6 (OPERACOES), e não base+subclasses do §9.7, porque aqui a tela é a mesma tela com outras palavras dentro; quem chama usa os construtores nomeados para_categoria/para_subcategoria, a convenção do PinPadDialog do §9.10. O SubcategoriaDialog foi APAGADO, não mantido ao lado. O achado do caminho é que as duas regras de nome repetido do service são DIFERENTES: categoria compara o nome exato (Categoria.nome == nome) e subcategoria compara pela chave_de_agrupamento (ignorando acento e caixa) — espelhar a regra de cada nível é o que impede a tela de mentir, porque um "✕ Nome já existente" ao digitar "lanches" com "Lanches" cadastrada seria um botão desligado por uma regra que o service não tem. Os dois testes ficam lado a lado, um afirmando o que o outro nega; a assimetria NÃO foi corrigida no service (é regra de negócio, e o pedido dizia para não mexer). Regra de negócio nenhuma mudou, método de service nenhum é novo e roteamento de impressão nenhum foi tocado — os 2 cupons saíram idênticos linha a linha, e o selo da impressora continua no cartão de contexto da subcategoria (fora do mockup, de propósito: é a regra de ouro do §9.8 dita em voz alta, e some na raiz, onde não há bobina a citar). O ciclo de vida do pedido item a item: quem destrói continua sendo executar_modal/descartar_modal (§3.2), atalho global nenhum existe para soltar (quem lê o teclado é o keyPressEvent do próprio diálogo) e _soltar_recursos() tira o filtro de eventos e desconecta as três ligações nominalmente, com a trava _limpo para a segunda passagem — trava que rendeu uma correção de teste registrada: o segundo disconnect NÃO levanta RuntimeError nesta versão do PySide6, devolve False e imprime RuntimeWarning, e o teste escrito contra a exceção passava verde com a trava apagada (o done() ainda carrega o @nao_deixa_escapar, que engoliria a exceção de qualquer jeito); ele passou a medir o aviso. A única coisa do pedido escrito que não foi seguida ao pé da letra é a largura: "entre 460 e 500px" não fecha, porque o rodapé de UMA linha que o mockup desenha pede 570px com a fonte da marca (Archivo Black, ~20% mais larga que a do desenho) — a 480px "+ Criar subcategoria" saía como "Criar subcategol" e "ENTER PARA CRIAR · ESC PARA FECHAR" virava "ENTER PARA CRIAR · ES". O cartão ficou nos 576px das próprias imagens, com a folga de 6px do pior caso trancada em teste que mede width() < sizeHint() de cada peça do rodapé, com a fonte da marca registrada na fixture (sem ela o offscreen não aperta e o teste passaria sem ter olhado, a armadilha do card de mesa do §9.5). Nenhum token de tema novo: a paleta "Concreto" já É a do mockup, e o resto veio das famílias cardapio_icone_*, cardapio_painel_bg e subcategoria_grupo_* — por isso o cartão nasce certo no tema claro também. Os glifos entraram no sistema de cardapio_cartoes.py (GLIFO_PASTA_MAIS, GLIFO_RAMO) e o _IconeDeRamo desenhado à mão do §9.9 deixou de existir; o corpo do cartão é um PainelPontilhado, como nas imagens. Na view, criar e editar CATEGORIA passaram ao while modal.exec() que a subcategoria já usava: o erro do service aparece dentro do cartão, sem redigitar (os reaproveitados foram de 4 para 6; o total de 36 sites de modal não mudou). Suíte 1530 (de 1495), 0 falhas, 56 testes novos, conferidos com 19 mutações (as 19 reprovam); bancada visual com as 24 telas IDÊNTICAS byte a byte e os 2 cupons idênticos; renderização nativa 576x374 nos dois níveis, nos dois modos e nos dois temas. Ficaram de fora: a exclusão (ainda QMessageBox do sistema), as pílulas "já existem nesta categoria" do §9.9 (a linha de validação faz o mesmo trabalho) e os três formulários de fábrica que sobraram no cardapio_view (_ProdutoDialog, _ComponenteDialog, _ComboComponentesDialog). Em 2026-09-12, ainda no Cardápio, o pedido dos três botões do rodapé — §9.13 do REMASTERIZACAO-V1.md: Editar/Desativar/Excluir estavam presos ao produto, e a subcategoria vazia (o estado de quem acabou de criá-la) ficava com os três apagados. Viraram sensíveis ao contexto: `_ProdutosPainel.alvo_atual()` devolve produto > subcategoria > categoria > nada, nessa precedência, e `_atualizar_barra_acoes()` redesenha rótulo, os três `setEnabled`, a palavra e a cor do botão do meio num lugar só. A precedência não é ambígua porque mover a árvore solta o produto. O rótulo virou "SUBCATEGORIA: COMBO PASTEL" no tom pleno do tema (token `texto`, que É o #FFFFFF pedido no Escuro — cravar o hex sumiria com a linha no Claro, a armadilha do §3.15), e o botão de religar usa os tokens da pílula Ativo (#4ADE80 no Escuro). Três despachantes em `_CategoriasPainel` (editar/alternar/excluir_selecionado) fazem o botão do rodapé, o menu de contexto e o F2 passarem pela MESMA rotina — antes o F2 editava a categoria mesmo com uma subdivisão destacada. Clicar no cabeçalho de um bloco (ou no aviso de bloco vazio) move a seleção da ÁRVORE em vez de a direita guardar contexto próprio, e é isso que torna a subdivisão sem produto alcançável pela direita; o link "Editar subcategoria" tem precedência dentro do cabeçalho e dispara a MESMA rotina do botão Editar. Duas decisões foram do Vitor, respondendo a perguntas feitas antes de começar, e as duas REVERTEM decisões escritas do §9.9. (1) Desativar subcategoria virou REGRA DE VENDA, igual à da categoria: coluna subcategorias.ativo + migração a3e6b91c4d05, com a regra num lugar só (ProdutoRepository._vendavel, compartilhado por listar_para_lancamento e listar_ativos_de_categoria_ativa). `outerjoin` e não `join`, senão produto sem subdivisão sumiria do balcão inteiro; e reativar o grupo NÃO religa quem foi desativado sozinho. (2) Excluir subcategoria com produtos dentro passou a ser RECUSADO com o número ("existem 3 produtos vinculados a ela"), e só a Senha Master (Nível 3, PinPadDialog.para_exclusao, barreira de TELA como no §9.10) libera a cascata: quem nunca foi vendido sai do banco, quem já tem venda ou está preso a um combo é ARQUIVADO (coluna produtos.arquivado, na mesma migração), tudo num commit só. `arquivado` NÃO é `ativo=0`: o desativado continua no Cardápio com selo para poder ser reativado, o arquivado some de todas as telas e não volta — reaproveitar `ativo` tiraria do gerente o caminho de reativar. `lancar_item` confere os dois, com mensagem própria para o arquivado. Achado no caminho: a confirmação de excluir CATEGORIA prometia apagar os produtos junto, e `excluir_categoria` faz o oposto (recusa categoria com produto dentro) — o texto passou a dizer o que o service faz. A REGRA DE OURO do §9.8 segue trancada: os 2 cupons saíram idênticos linha a linha. Suíte 1596 (de 1530), 66 testes novos, conferidos com 24 mutações (23 reprovam; as sobreviventes da primeira passada viraram correção e não teste: ativar_produto aceitava arquivado — virou recusa —, e saíram dois filtros/atribuições que não filtravam nem atribuíam nada, o `arquivado` do contar_produtos e o `_selecao = _SUB_TODAS` que repetia o `reserva` do §9.11); memória sobre o cardápio real com +0,9 MB e zero widget novo (72→72) em 60 voltas de desativar/ativar e 40 de criar+excluir subcategoria; bancada visual a 1366x738 com 22 das 24 telas idênticas byte a byte, as duas do Cardápio diferindo SÓ na faixa do rodapé (y=659..695). Ficaram de fora: desarquivar pela tela (não existe lixeira), registro de quem arquivou o quê (não há tabela de log no projeto), filtro/relatório por subcategoria desativada, e a exclusão de categoria continua no QMessageBox do sistema. Ainda em 2026-09-12, logo depois, o pedido de excluir a CATEGORIA inteira — §9.14 do REMASTERIZACAO-V1.md: "deixando excluir toda uma categoria por mais que tenha itens e subclasses, mas deverá gerar uma tela de senha de pin exigindo a senha master". É o §9.13 um nível acima, e o nível acima esbarra numa restrição que desenha o item inteiro: produtos.subcategoria_id é ANULÁVEL (SET NULL), então a subdivisão sempre sai do banco; produtos.categoria_id é NOT NULL (escolha do §9.8: produto sem categoria não tem impressora), então um produto que precisa sobreviver à exclusão porque já foi vendido TEM que continuar apontando para alguma categoria. Daí a cascata da categoria ter dois finais: sem nada com histórico, a categoria é apagada de verdade e as subdivisões vão junto pelo cascade da relação; com algo com histórico, ela FICA MARCADA (coluna categorias.arquivado + migração c7b4e0f12a86), some de todas as telas, e é a linha dela que sustenta a venda antiga. O detalhe que morderia depois é o nome: categorias.nome é UNIQUE, e a guardada continuaria ocupando "Lanches" — justamente o nome que se recadastra ao reorganizar o cardápio. Quem libera é o service, renomeando a linha para "Lanches [excluída #3]" (o id dá unicidade por construção; o corte a 80 tira o fim do nome e nunca o marcador). Tirar a UNIQUE do banco foi RECUSADO: exigiria recriar categorias inteira com produtos e subcategorias apontando para ela e o foreign_keys=ON ligado, o perigo do cabeçalho da f8d1a6c40b27. "Vazia" ficou mais estrito que na subcategoria — nem produto nem subdivisão: as subdivisões sempre foram junto pelo cascade, e irem junto EM SILÊNCIO num clique desmanchava a organização de um grupo inteiro. A recusa soma as duas parcelas ("ela tem 1 produto e 2 subcategorias"). _categoria_viva() é o guarda por onde passam editar, ativar, desativar, associar impressora, criar subcategoria e criar/atualizar produto: a guardada não volta, porque reativá-la traria um grupo cujos itens continuam arquivados. Suíte 1628 (de 1596), 32 testes novos, 18 mutações e as 18 reprovam (a sobrevivente da primeira passada — o filtro de arquivado em listar_ativas, redundante porque a cascata desliga ativo junto — virou o teste que prende o cinto). Bancada visual com as 24 telas IDÊNTICAS byte a byte às do §9.13, que é o esperado: o item vive em diálogos e a tela parada não mudou um pixel; 2 cupons idênticos; boot em banco novo com head c7b4e0f12a86. De quebra, uma mutação achou um defeito do PRÓPRIO teste: o helper encenar() do §9.13 TRAVAVA quando o diálogo aberto era de tipo diferente do esperado pelo roteiro — ganhou teto de espera. Ficaram de fora: desarquivar categoria pela tela (não existe lixeira), escolher para onde mover os produtos com histórico (seria mais uma decisão no meio de um gesto destrutivo) e o relatório de vendas por categoria, que é o que daria valor a preservar a associação do produto arquivado com a categoria guardada. Em 2026-09-13, o pedido da composição do combo em cartão — §9.15 do REMASTERIZACAO-V1.md: o `_ComboComponentesDialog` (QTableWidget de duas colunas, onde mudar a quantidade era remover e associar de novo, dois commits com o combo incompleto no meio) virou src/gestor_comercial/ui/widgets/composicao_combo_dialog.py, o NONO modal em cartão, com uma linha por componente e stepper na própria linha. Duas decisões do Vitor, perguntadas antes de começar: (1) "Produto principal/Adicional" do mockup NÃO existe em combo_itens, e em vez de heurística ou coluna nova o subtítulo é a CATEGORIA real do componente e o selo âmbar com visto marca a linha SELECIONADA (a que o Remover tira) — schema intacto, sem migração; (2) o "+ Adicionar componente" reusa o cartão "Adicionar item" num modo componente (`MODOS`/`ModoDeLancamento`, construtor `para_componente`): sem observação e sem prévia em R$ (não criadas, não só escondidas), teto 99, adição sequencial com o cartão aberto; o modo comanda é o padrão e não mudou. Único método novo de service: `alterar_quantidade_componente`, com as travas de `associar_componente`; o número do stepper só muda depois de o service aceitar. Linhas com widget (têm controle de verdade), sincronizadas pelo par (componente, produto) porque o SQLite reaproveita o maior id; linha removida é destruída na hora (setParent(None) + deleteLater); ↑/↓, Delete/Backspace, Esc; a view só recarrega o Cardápio se `modal.alterou`. Largura 640px e não "560–600": o rodapé de uma linha pede 635px com a fonte da marca (mesmo achado do §9.12). Impressão e estoque não leem combo_itens na V1 (a baixa de estoque é backlog da V2): nada foi tocado, e uma venda com combo imprimiu pedido, pré-conta e recibo idênticos ao commit anterior, além dos 2 cupons da bancada. Achados, todos com teste: o Backdrop de um cartão sobre outro cartão pintava os cantos transparentes (alfa 150→0, `cartao_modal.RAIO_CARTAO_PX`); a barra de rolagem deslocava os steppers (`_acomodar_barra`); border-radius maior que meia altura deixa o botão quadrado; N+1 em `listar_por_combo` (selectinload, 3 consultas fixas) e `remover_componente` passou a usar o EXISTS; showEvent duplicado com o do cpf_dono_dialog saiu para `cartao_modal.apresentar`. Apagados: `_ComboComponentesDialog`, `_ComponenteDialog` e o `BuscaProdutoWidget`, que ficou sem uso. Suíte 1698 (de 1628), 0 falhas, 70 testes novos; 30 mutações, 25 reprovaram na primeira passada e as 5 sobreviventes viraram 4 testes e 1 remoção de código — a séria era tirar o commit do stepper, que passava a suíte inteira (a leitura na mesma Session ainda via o valor; o teste agora faz rollback e confere). Memória em banco de arquivo com WAL/FULL: widgets vivos 1→1 em 240 ciclos completos, RSS +0,05 a +0,17 MB por bloco de 60 depois do aquecimento; clique no stepper 2,5 ms com commit em disco. Bancada visual: as 24 telas idênticas byte a byte. Ficaram de fora: reordenar componentes, confirmação ao remover (a tabela antiga não tinha) e o `_ProdutoDialog`, último formulário de fábrica do Cardápio. Ainda em 2026-09-13, o relato das fotos do Cardápio — §9.16 do REMASTERIZACAO-V1.md: no bloco "Sucos e Vitaminas" as miniaturas saíam cada uma de um tamanho e por cima do nome, e um `.avif` dava erro. O estouro era do thumbnail_cache, que ESCALAVA a foto com KeepAspectRatioByExpanding sem cortar (só o círculo cortava), e no banco real 18 das 35 miniaturas em disco não eram quadradas — a 120x67 virava 64x36 a 36px e os 28px de sobra caíam no nome. O pipeline é QImage, não Pillow, e o Qt do projeto não lê AVIF/HEIC. `ler_quadrado_central` (services/imagem_service.py) faz o enquadramento do ImageOps.fit pedido ao próprio QImageReader (setClipRect + setScaledSize) e nunca devolve outro tamanho; quem grava e o cache usam a mesma função, então as miniaturas antigas ficaram certas sem regravar. Disco: exatamente 120x120 JPEG, achatado sobre branco se tiver alfa (achado da sonda: PNG transparente virava quadrado PRETO). EXTENSOES_ACEITAS (png/jpg/jpeg/webp) gera o FILTRO_DO_SELETOR e é conferida no service, porque o seletor do Windows aceita `*.*` e caminho colado; recusa e `.avif` renomeado viram a mesma frase ("Formato inválido. Selecione PNG, JPG ou WEBP.") numa linha do _ProdutoDialog, sem QMessageBox e sem tocar nos campos, com o motivo no gestor.log. Foto e placeholder em cartão com cantos de 8px e borda borda_card, mesma silhueta; letra do placeholder em texto_fraco (#A1A1AA no Escuro); setClipRect no delegado como segunda trava. Não seguido ao pé da letra, com o porquê no §9.16: Pillow (o leitor do Qt decodifica o JPEG já reduzido — escolher uma foto de 12 MP levava o processo de 64 a 110 MB e agora vai de 65 a 68 MB), LRU de 100 (ficou 200: 113 produtos × dois lados), deleteLater por linha (o Cardápio não tem widget por linha desde o §9.11) e 44px/14px (a 125% os 36+12 lógicos da linha já são 45+15 físicos). Suíte 1773 (de 1698), 75 testes novos, 13 mutações e as 13 reprovam; renderização com o banco e as fotos reais reproduz a captura no antes e corrige no depois, nos dois temas; bancada com 18 das 24 telas idênticas byte a byte, as 6 restantes diferindo só na coluna das miniaturas. Ficaram de fora: regravar as miniaturas antigas (as que vieram de PNG transparente seguem com fundo preto até nova escolha), nitidez em tela de 125% e o _ProdutoDialog em cartão. Ainda em 2026-09-13, o relato do acordeão do Cardápio — §9.17 do REMASTERIZACAO-V1.md: clicar numa categoria só expandia, e clicar de novo na aberta não recolhia. _ao_clicar passou a alternar (aberta recolhe com a seleção na linha da categoria, que é o "Todas" dela; fechada abre por _abrir_somente), valendo na linha inteira (seta, nome e contador são pintura da mesma linha). Inverter a condição não bastava, por duas coisas do Qt medidas numa sonda: recolher o pai deixa o "atual" preso num filho invisível, e setCurrentItem num filho de ramo fechado REABRE o ramo (a docstring de _primeiro_filho_visivel dizia que o Qt recusava — corrigida). A recarga tratava "selecionada" como "aberta" e reabriria a recolhida: _expandida_id None com categoria selecionada em "Todas" virou esse estado, e criar categoria, criar subcategoria e clicar num cabeçalho de bloco abrem a categoria explicitamente. Na direita, ampliar de uma subdivisão para "Todas" na MESMA categoria (que é o que recolher faz) mantém o produto escolhido e, sem produto, ancora a rolagem no bloco de onde o gerente veio; estreitar e trocar de categoria continuam soltando (§9.13). Não seguido ao pé da letra, com o porquê no §9.17: destruir sub-itens ao recolher (a árvore não tem widget por linha desde o §9.11; recriar a cada abertura seria mais alocação e quebraria a busca por subdivisão), lambda/after_cancel (idiomas de Tkinter; não há sinal por linha nem timer, isAnimated() é False) e os ~90 MB (o gesto acrescenta zero). Suíte 1794 (de 1773), 21 testes novos com clique de mouse de verdade, 16 mutações e as 16 reprovam; no cardápio do seed, 1.000 cliques com 0 SQL, widgets 72→72, QObjects 117→117 e RSS plano depois do primeiro bloco; bancada com as 24 telas idênticas byte a byte. Ficou de fora: o teclado (← e → abrem pelo Qt, por fora do acordeão). Em 2026-09-14, o pedido dos três mockups de exclusão do Cardápio — §9.18 do REMASTERIZACAO-V1.md: as seis caixas de mensagem do sistema (categoria vazia/cheia, subcategoria vazia/cheia) e o "Sim/Não" do produto viraram UM cartão, src/gestor_comercial/ui/widgets/confirmacao_exclusao_dialog.py, o décimo modal em cartão, parametrizado por dois eixos em duas tabelas: ENTIDADES (glifo, rótulo, título, palavra do botão) e NIVEIS (tarja, tom coral/âmbar e o que o botão faz — SIMPLES confirma, SENHA_MASTER pede o PIN, BLOQUEADA nem liga). Quem transforma contagem em nível são os construtores nomeados para_categoria/para_subcategoria/para_produto, e a view lê modal.protecao para escolher o service — a regra que escolhe o cartão e a que escolhe o service são a mesma linha. A tarja é do nível e não da entidade (as três imagens fecham assim, e só assim o cartão não chama de "ação permanente" um produto que vai ser arquivado). A Senha Master abre POR CIMA do cartão: a view entrega `autorizar` (partial de _pedir_senha_master, PinPadDialog.para_exclusao filho do cartão), e PIN recusado ou fechado devolve o gerente ao cartão; o diálogo não conhece AuthService e recusa nascer SENHA_MASTER sem barreira. Duas decisões do Vitor, perguntadas antes de começar: (1) subcategoria cheia mantém a cascata com Senha Master do §9.13, e não o bloqueio da Foto 3 (o modo bloqueado existe, cascata=False, sem uso na tela); (2) produto com histórico (venda ou combo) passa a ser ARQUIVADO com a Senha Master em vez de recusado com "Desative-o": CardapioService.arquivar_produto desfaz as linhas de combo_itens dos dois lados, devolve a produto comum o combo que ficou sem componente (_desfazer_combo_sem_componentes, compartilhado com remover_componente) e marca arquivado+ativo=False num commit só; VinculosDoProduto/vinculos_do_produto dão os números que o cartão diz, e _tem_historico das cascatas passou a ler a mesma definição. Sem migração. Enter confirma (pedido; inverte o botão padrão dos QMessageBox), ⚠ e 🗑 desenhados (GLIFO_ALERTA; BotaoComGlifo saiu do combo para cardapio_cartoes), 500px de largura, tokens exclusao_* nos dois temas, nome e aviso quebram linha em vez de reticências. Sites de modal 40→38. Achado e registrado como tarefa separada, sem corrigir aqui: o cartão do §9.12 reaberto no while modal.exec() depois de erro do service fica com ✕/Cancelar/Salvar mudos (done() desconecta os botões). Suíte 1893 (de 1794), 0 falhas; 30 mutações, as 30 reprovam (a sobrevivente da primeira passada, PIN filho da tela e não do cartão, virou teste); 300 ciclos cartão→PIN→cartão com widgets 73→73, QObjects 117→117, 0 diálogos vivos e RSS plano depois do primeiro bloco; cartão monta e pinta em ~43 ms; 24 telas idênticas byte a byte e 2 cupons idênticos contra b3e851f. Ficou de fora: as cascatas continuam arquivando componente SEM desamarrar o combo (trancado em teste, decisão pendente do Vitor) e o erro do service depois do cartão fechado segue na linha vermelha da tela. Em 2026-09-15, o pedido do cadastro de impressora em cartão — §9.19 do REMASTERIZACAO-V1.md: o `_ImpressoraDialog` de fábrica (QFormLayout, combo de 5 tipos, QSpinBox de colunas, checkbox só na edição) virou src/gestor_comercial/ui/widgets/impressora_dialog.py, o 11º modal em cartão, UMA classe para "Nova" e "Editar" (`MODOS`, `para_nova`/`para_editar`, e `modo="novo"/"editar"` aceito no __init__). Três decisões do Vitor, perguntadas antes de começar: (1) o card USB é a CONEXÃO LOCAL — o campo lista as filas do Windows e as portas COM (hardware/descoberta_local.py, lido numa thread de trabalho pela porta estática ImpressaoService.listar_destinos_locais, conferido num QTimer de 50ms; filas virtuais como PDF/OneNote ficam de fora pela porta) e aceita digitar COM3 ou vendor:product; grava WINDOWS/SERIAL/USB, sem detecção automática na impressão e sem tocar impressora_escpos.py; (2) "Uso da impressão" grava só a marca de padrão — "Recibo do cliente" = padrao=True com a antiga perdendo a marca no MESMO commit, "Produção (por categoria)" = False, Copa/Bar fora, schema intacto; (3) "Impressora ativa" funciona nos dois modos: criar_impressora ganhou ativa=True e padrao=None, editar_impressora ganhou padrao=None, e as regras da marca (desligada nunca é padrão; só uma marcada) foram para CardapioService._aplicar_uso, que definir_padrao também usa. O cartão grava pela função `salvar` da view e só fecha se o service aceitou (erro fica dentro do cartão; uma abertura só, sem o while do §9.12); o veredito à direita do resumo avisa "Sem impressora de recibo"; bobina 58/80mm preserva as colunas cadastradas (42 continua 42) e abrir-e-salvar-sem-mexer devolve o mesmo cadastro nos cinco tipos. Sites de modal 38→37. Achados: o QDialog::setVisible manda um FocusIn "de Tab" depois do showEvent e o nome abria selecionado (virou override de setVisible); o helper de teste travava a suíte com o cartão aberto (ganhou vigia de 3s). Suíte 2045 (de 1893), 152 testes novos; 35 mutações, as 35 reprovam; 300 ciclos com a busca real do Windows: widgets 1→1, 0 diálogos, threads 1→1, RSS convergindo; 2 cupons e 24 telas idênticos contra 2c7dfa1. Em 2026-09-16, o campo de dinheiro — §9.20 do REMASTERIZACAO-V1.md: preço de venda e preço de custo do "Novo produto"/"Editar produto" eram QLineEdit crus que aceitavam "12,5abc" e só reclamavam no OK. Viraram src/gestor_comercial/ui/widgets/campo_moeda.py (CampoMoeda, que continua sendo QLineEdit, então o QSS de campo e o _marcar_erro valem sem cópia), com a regra pura sanitizar_edicao_moeda em ui/formatacao.py ao lado de safe_decimal. A regra mora num QValidator que REESCREVE o texto e nunca devolve Invalid, e não num keyPressEvent: o validador é a porta única de tecla, Ctrl+V, Shift+Insert, "Colar" do menu de contexto (ligado em C++ ao slot paste(), fora do alcance de override Python), arrastar e setText; com Invalid o Qt desfaz a tecla mas NÃO o setText. Texto sempre canônico (8 dígitos inteiros pelo dinheiro.LIMITE, uma vírgula, 2 casas) e ponto fixo (a segunda validação devolve igual, então não há laço). O que a edição TROUXE se separa do que já estava pelo último texto aceito + cursor do Qt: a vírgula que já estava vence (sem isso "12,5" + "." seria milhar e viraria 125), ponto vira vírgula, colagem com milhar é lida como a safe_decimal lê ("R$ 1.234,56" → "1234,56"), no excesso sai o que foi trazido, e lixo colado sobre uma seleção não apaga o valor. "R$" é QLabel filho na margem do texto (o cursor não o alcança), transparente ao mouse, texto_fraquissimo em repouso e acento no foco (#71717A/#E5A93C no Escuro, nenhum token novo); ao sair do campo "12" vira "12,00" e vazio fica vazio (o menu de contexto não formata). Rótulos "Preço de venda"/"Preço de custo", e vazio e zero ganharam frases próprias. Não seguido ao pé da letra: parse_moeda_para_float não foi criada (dinheiro() recusa float com TypeError, e a conversão pedida já é a safe_decimal) e o fundo #111110 dos modais em cartão não foi aplicado a um formulário que ainda é de fábrica. Service intocado. Suíte 2128 (de 2045), 83 testes novos; 30 mutações, 29 reprovam (a declarada é o ensurePolished antes de medir o "R$": no offscreen as duas fontes caem no mesmo arquivo e medem 17px); 5,4 µs por tecla na regra e 37 µs no evento completo, zero objeto por tecla; 300 ciclos do cadastro com 0 diálogos vivos e memória privada plana; 24 telas idênticas byte a byte contra e432a57. Ficaram de fora: o valor do PagamentoDialog e do _QuitarConsumoDialog (uma linha cada, não pedido) e o _ProdutoDialog em cartão. Ainda em 2026-09-16, o .exe de produção — §9.21 do REMASTERIZACAO-V1.md: ícone oficial novo, cardápio embutido e pasta de dados própria, num fluxo só (`.venv\Scripts\python.exe packaging\gerar_exe.py`: suíte → `pyinstaller --clean packaging\app.spec` → prova de fumaça do .exe real). A máquina do pai já rodou o .exe de testes de 2026-09-01, que gravou em `~/.gestor_comercial`; o .exe novo grava SÓ em `%APPDATA%\GestorComercial_V2\`, sem fallback, e ignora `GESTOR_COMERCIAL_DB` (em dev nada mudou). A regra "onde fica a pasta de dados" existia em quatro cópias (banco, log, fotos, cupons — a dos cupons ignorava a variável) e virou `core/caminhos.py`, com varredura na suíte. O DEPLOYMENT.md antigo mandava copiar o .db à mão, e isso perdia o `-wal`: medido neste dia, a cópia só do .db levaria 182 produtos em vez de 188, 169 fotos em vez de 177 e 8 itens de combo em vez de 29, sem erro — e as fotos, que moram em `uploads/thumbnails`, nem iam. Agora a SEMENTE é gerada a cada build (`repository/preparo_da_semente.py`, chamado pelo spec): API de backup do SQLite por conexão somente leitura, migration na cópia, `journal_mode=DELETE` + VACUUM + integrity/foreign_key_check, só as fotos que algum produto usa; o build PARA se o banco de trabalho tiver qualquer linha de venda/caixa (um turno de teste esquecido faria o programa do pai nascer com caixa aberto) ou foto faltando, sem apagar nada. No boot, `core/banco_semente.py::provisionar` roda antes de qualquer conexão: banco existe → um stat e nada mais; não existe → sob trava de arquivo do SO (duplo clique durante a extração do .exe único), fotos primeiro, `-wal/-shm/-journal` órfãos apagados, banco por último via temporário + fsync + os.replace (o banco é o ponto de confirmação: queda no meio refaz do zero). Ícone: `packaging/gerar_icone.py` virou conversor do PNG oficial (a arte veio sobre fundo branco; cantos viram transparentes por conectividade + "cor para alfa" no anel), `resources/icons/app_icon.ico` 16–256px, `app.ico` apagado; AppUserModelID `gestor.comercial.pdv.v2` no boot e o mesmo nos atalhos do `instalador.iss` (teste confere). Binário: poda do Qt por alcançabilidade no grafo de imports do PyInstaller (fora opengl32sw, traduções, Quick/QML/PDF/OpenGL/teclado virtual e o OpenSSL do Qt; pygments/setuptools/numpy/AVIF do Pillow excluídos): conteúdo extraído a cada abertura 136,3 → 81,0 MB, .exe 69,1 → 41,3 MB. Suíte 2157 (de 2128), 29 testes novos, 15 mutações e as 15 reprovam; prova de fumaça (máquina de dev): janela em 6,5 s na primeira abertura e 4,5 s na segunda, 188 produtos e 177 fotos iguais à semente, isca da variável não criada, banco de trabalho intocado, banco preservado na reabertura (depois de encerramento forçado). Ficaram registrados: as senhas viajam dentro do .exe junto com o cardápio (não distribuir o .exe além da máquina do pai; trocar as senhas lá na primeira abertura), o instalador não foi compilado e o formato continua arquivo único. Em 2026-09-19, a conferência da mesa em cartão e a taxa de serviço contada à parte — §9.23 do REMASTERIZACAO-V1.md: o `_FecharConferenciaDialog` de fábrica do "Fechar conta" virou src/gestor_comercial/ui/widgets/conferencia_dialog.py (`ConferenciaMesaDialog`, 12º modal em cartão, 576px), com subtotal, total da pré-conta e a caixinha da taxa de 10% que recalcula na hora — abre DESMARCADA (o Vitor primeiro respondeu "marcada", depois trocou no meio do trabalho: o operador marca só quando o cliente vai pagar). O cartão recebe `PreviaDeConferencia` (instantâneo de `ComandaService.previa_de_conferencia`) e cem cliques são zero SQL. A taxa em reais passou a ser GRAVADA isolada (`comandas.valor_taxa_servico`) no mesmo commit do percentual e do status, por `valor_da_taxa_de_servico`, a única conta de taxa do sistema; total a pagar e cupom leem o valor gravado; `reabrir` zera. A Central de Loja ganhou o interruptor "Cobrar taxa de serviço (10%)" na tela de Configurações (`loja_config.aceita_taxa_servico`, ligada por padrão); com ela desligada o bloco da taxa nem é criado e o service recusa a taxa antes de mexer na comanda. Migração e4b8c2a6d913 (as duas colunas, com BEGIN explícito e preenchimento das contas antigas em Decimal). O `_Interruptor` do cartão de impressora saiu para ui/widgets/interruptor.py (tokens `interruptor_*`). O fechamento do dia mostra a taxa como FATIA do faturamento ("inclusa"): card Recebimentos do Caixa, seção TAXA DE SERVIÇO do comprovante, linha no fechamento impresso e painel Fechamento da Gaveta dos dois relatórios, somada na mesma consulta da contagem de comandas. Pré-conta imprime "Taxa de serviço (10%)" (antes "10,00%"). O PagamentoDialog não foi tocado. De quebra: `test_caos` já falhava no HEAD 831aa88 (`minimumSizeHint` do cartão de impressora sem `@nao_deixa_escapar`) e foi corrigido. Suíte 2388 (de 2286), 102 testes novos, 29 mutações e as 29 reprovam. Ainda em 2026-09-19, a grade de mesas sem rolagem para o lado — §9.24 do REMASTERIZACAO-V1.md: com `_COLUNAS_GRADE = 8` num QGridLayout a grade pedia 884px e a 1366px a área só tem 710 — 174px de barra horizontal e 14 das 60 mesas cortadas (21 a 1280). Virou `GradeFluida` (ui/widgets/flow_layout.py, filha do FlowLayout): colunas = (largura + vão) // (mínimo + vão), células iguais esticadas até a borda (a sobra vai 1px para as primeiras colunas, o vão é sempre 14px), mínimo de UMA célula — rolagem horizontal impossível, e não só escondida; a barra fica em ScrollBarAlwaysOff por cima. 1280/1366/1440/1600/1920 dão 5/6/7/8/11 colunas, nenhuma mesa cortada. A célula vem do MÍNIMO do cartão e não do conteúdo (uma comanda de R$ 1.000 não pode tirar uma coluna e mudar a mesa de lugar). Medida da célula guardada até o Qt invalidar (heightForWidth e minimumSize viram aritmética); redimensionar só reposiciona. Achado: cada recarga fazia 61 relayouts da grade, um por cartão (o show() agendado de cada widget novo ativa o layout do pai; já era assim com o QGridLayout, em C++) — `GradeFluida.repovoar` desliga o layout, exibe os cartões e religa: 1 relayout, recarga 46 → 43ms; redimensionar 7,5 → 7,9ms (quase tudo repintura; a grade antiga nem redimensionava cartão abaixo de 1600px). Era também o motivo do PNG de Mesas da bancada mostrar cartões sobrepostos. Suíte 2408 (de 2388), 20 testes novos em tests/ui/test_grade_de_mesas_fluida.py, 16 mutações e 15 reprovam (a declarada é o minimumSize, desempenho puro); 22 das 24 telas idênticas byte a byte nos dois tamanhos. Ficou para o Vitor decidir: o corte de texto do cartão ocupado é anterior e dependia da resolução — antes, a 1366, todo valor a partir de R$ 100 saía cortado; agora cabem até R$ 999,99, e seguem cortados R$ 1.000+ e "Turno - Manhã" (LARGURA_MINIMA_PX = 96 é a régua). E o HEAD 831aa88 não importa sozinho: impressora_escpos.py commitado importa core/caminhos.py, ainda não commitado (§9.21). O `.exe` precisa ser gerado de novo para levar isto. Em 2026-09-20, o fechamento de mesa sem pergunta e o recebimento em tela cheia — §9.25 do REMASTERIZACAO-V1.md, no dia seguinte ao §9.23 e desfazendo parte dele: a caixinha da taxa "deixa de existir como etapa de escolha do operador". O cartão de conferência CONTINUA (decisão do Vitor, perguntada entre "um clique sem tela nenhuma" e "o cartão só confirma": um clique errado tranca a mesa e só o PIN de gerente reabre), mas sem controle nenhum — quem decide se a loja cobra os 10% é a Central de Loja, e o cartão abre com a conta pronta. `PreviaDeConferencia.total` e `percentual_a_gravar` viraram propriedades sem parâmetro, a caixinha virou selo e o Espaço não alterna mais nada. O `PagamentoDialog` (último diálogo de fábrica do fluxo de venda: combo de forma, campo de texto, PIN e resumo de três linhas) virou src/gestor_comercial/ui/views/pagamento_view.py, uma TELA no QStackedWidget com "← Voltar à mesa" e "Imprimir 2ª via" no cabeçalho: à esquerda o consumo (itens, subtotal, serviço, o bloco âmbar do total e o "dividir por", que só MOSTRA quanto dá por pessoa, arredondando para cima), à direita as cinco formas em cards (Dinheiro, Pix, Débito, Crédito e "Consumo", que é o consumo interno de sempre — PIN de gerente Nível 2 e saldo devedor do funcionário), o valor no CampoMoeda do §9.20, o troco ao vivo (só em dinheiro, que é a única forma que o service aceita acima do restante) e o card da comissão. A tela recebe um `ContaParaPagamento` imutável (`PagamentoService.conta_para_pagamento`) e não lê `Comanda` na pintura. A COMISSÃO é a taxa: não há coluna de valor, porque a comissão de uma conta É o `valor_taxa_servico` dela (§9.23) — a migração f6a1d3b78c42 acrescenta só o controle do repasse (`comandas.comissao_paga` + `comissao_paga_em`), com o passado entrando como ACERTADO (nascer pendente abriria uma lista de dívidas de todas as vendas antigas). Os botões são "Comissão paga" e "Comissão não paga" (nomes do Vitor), e o padrão é NÃO PAGA, confirmado por ele: nenhum dinheiro sai da gaveta sem um clique deliberado. "Paga" grava a marca e chama `CaixaService.registrar_repasse_comissao` no MESMO commit que fecha a conta (marcada sem saída faz o turno sobrar dinheiro; saída sem marca paga o garçom duas vezes), e só quando a conta entrou em dinheiro — no cartão ou no Pix aquele dinheiro não está na gaveta. `TipoMovimento.COMISSAO` entrou em TIPOS_QUE_SAEM_DA_GAVETA e em TIPOS_FORA_DO_LANCAMENTO_MANUAL (ao lado do consumo de funcionário: comissão digitada à mão não teria comanda do outro lado); a coluna `movimentos_caixa.tipo` é VARCHAR sem CHECK, então o tipo novo não pediu migração. O repasse aparece no card "Ajustes do turno" do Caixa, na seção de saídas do comprovante, na linha "Comissões repassadas" do fechamento impresso e no `ResumoCaixa.comissoes` que os três leem. Em Funcionários entraram o KPI "Comissão a repassar", o card no perfil do garçom e o botão que acerta tudo dele de uma vez (`pagar_comissoes_do_funcionario`), com UM movimento de gaveta pela parte que entrou em dinheiro. Novo em `PinPadDialog`: `para_consumo_interno` e a propriedade `pin_confirmado`, o único caminho que devolve o PIN digitado — quem grava é o service, que reconfere a credencial. Glifos novos (cédula, cartão, celular, pessoa) e família de tokens `pagamento_*` nos dois temas. Suíte 2453, 0 falhas, 50 testes novos, 32 mutações e as 32 reprovam. A renderização pegou o que os testes não pegavam (a coluna da direita espremida, com os cards de forma de pagamento sem texto), corrigido com piso de altura, margens menores e rolagem — e o teste de aperto passou a medir ALTURA também. De quebra, três achados na própria suíte, que tinha ficado 8x mais lenta: a fixture do cartão de impressora chamava o SPOOLER DO WINDOWS de verdade a cada teste (agora a lista falsa é o padrão), os testes de aperto deixavam uma janela visível e ATIVA para trás (roubava o foco do teste seguinte e o fazia levar 6 minutos) e os helpers de diálogo esqueciam um QTimer de 0ms ligado (viraram gerenciadores de contexto). O `.exe` precisa ser gerado de novo para levar isto. Em 2026-09-21, a taxa de serviço SAIU do sistema inteiro — §9.26 do REMASTERIZACAO-V1.md, desfazendo o §9.23 e parte do §9.25: o total da conta é a soma dos itens (menos o desconto, que continua), e nada mais. A comissão do garçom saiu junto, por decisão do Vitor, porque nunca teve valor próprio (ela ERA o `valor_taxa_servico` da conta). Migração c5d9e17a24b8: cinco DROP COLUMN nativos (`comandas.taxa_servico_percentual`, `valor_taxa_servico`, `comissao_paga`, `comissao_paga_em` e `loja_config.aceita_taxa_servico`) e o movimento `COMISSAO` reescrito como `DESPESA` (o tipo saiu do enum, e o dinheiro saiu da gaveta de verdade). `pagamentos` intocado: o faturamento não muda. Saíram o interruptor da Central de Loja, o selo da taxa (o cartão de conferência mostra UM valor, o total), a linha "Serviço" e o card da comissão da tela de pagamento, o KPI e o repasse de Funcionários, e as linhas de taxa/comissão do Caixa, dos relatórios, da pré-conta, do fechamento impresso e do comprovante. Suíte 2354 (de 2453), 0 falhas; a migração rodou numa cópia do banco de trabalho com os números idênticos antes e depois. O `.exe` precisa ser gerado de novo. Ainda em 2026-09-21, a tela da mesa em duas colunas — §9.27 do REMASTERIZACAO-V1.md, pedido do Vitor com mockup no padrão "Solvix POS": a `ComandaView` (841 linhas, seis pílulas do mesmo peso no cabeçalho, duas tabelas com widget por célula e a barra TOTAL no pé) virou src/gestor_comercial/ui/views/mesa_detalhe_view.py (`MesaDetalheView`), com as peças em ui/widgets/: itens_da_comanda.py (`CartaoDeItens` + `LinhaDeItem`, o mesmo cartão para "Aguardando envio" e "Itens em produção"), comanda_resumo.py (`ComandaResumoWidget`, TOTAL ESTIMADO = soma dos itens, sem taxa), painel_acoes.py (`PainelAcoesWidget`, cinco sinais e nenhum service), esteira_de_status.py (Atendimento → Produção → Conferência → Pagamento) e seletor_de_garcom.py. Regra de negócio nenhuma mudou: cada botão chama o MESMO método de service de antes, e a `MainWindow` só trocou o nome. Decisão do Vitor, perguntada antes: o Reabrir (fora do mockup e da lista) ganhou botão próprio, só em conferência — e o "Fechar para conferência", sempre apagado ali, some em conferência, porque com os cinco botões a coluna rolava a 1366x738 e escondia o "Cancelar comanda". A tela lê um instantâneo imutável, `ComandaService.painel_da_comanda` → `PainelDaComanda` (itens agrupados com os ids por baixo, totais, garçom, `EtapaDaComanda`), com `listar_ativos_com_produto` (selectinload): 5 consultas fixas por recarga, onde a antiga fazia 9 com 4 itens e 25 com 20 (80,9 → 10,4 ms). Linhas recicladas, ligadas uma vez, e as que sobram destruídas na hora na troca de mesa (desligadas, sem pai, deleteLater); widgets e objetos Python planos em 1.000 trocas. O combo do garçom só lê os funcionários ao abrir. DRY com a tela de pagamento: badge_com_glifo, rolagem_vertical e rotulo_do_operador saíram de dentro dela (tela de pagamento idêntica byte a byte). Achado e corrigido: o "Enviar e Sair" da tela antiga NÃO saía (decidia pela linha de mensagem vazia, e o envio que dá certo escreve nela) — provado com sonda antes de apagar, com teste de regressão. Família de tokens `mesa_detalhe_*` (35) e 18 tokens + os seletores da tela antiga removidos; `hora_primeiro_envio` saiu (sem leitor). Suíte 2449 (de 2354), 95 testes novos; 33 mutações, as 33 reprovam; 22 das 24 telas da bancada idênticas byte a byte (só as duas da comanda diferem) e 2 cupons idênticos. O `.exe` precisa ser gerado de novo. Ainda em 2026-09-21, dois defeitos relatados juntos — §9.28 do REMASTERIZACAO-V1.md. (1) O tema escolhido em Configurações se perdia ao fechar o programa (o `ThemeController` só guardava em memória): agora é uma linha em `preferencias` (chave `tema`, `claro`/`escuro`, sem migração), lida e gravada por `services/preferencia_service.py`; `main.py` chama `ThemeController.restaurar` depois das migrations e ANTES da `MainWindow` (as telas nascem na paleta certa, uma leitura por chave primária), e todo `alternar_para` grava antes de avisar as telas, degradando para o log se o disco falhar. (2) O mesmo turno tinha dois nomes: o cabeçalho, a tela de Caixa, os relatórios e o comprovante nomeavam pela HORA (o login "Caixa Turno - Noite" abriu às 17:36 e o cabeçalho dizia "Caixa Turno - Tarde"), as telas de venda pelo operador, e o rótulo só se atualizava na navegação. Decisões do Vitor: o nome do turno é o que ele edita em Funcionários ("deve ser o que eu editar e deixar editado") — `caixa_service.nome_do_turno` é a regra única (o nome do operador que abriu; a hora só para turno sem operador), desfazendo a regra de hora do §3.1 — e editar o funcionário renomeia o login do par no mesmo commit (`FuncionarioService.editar`, recusando o nome de outro login). Na tela, `ui/rotulo_identidade.IdentidadeDoTurno` é a fonte única: login/logout, `CaixaView.turno_alterado` e `FuncionariosView.cadastro_alterado` chamam `recalcular`, e `rotulo_mudou` chega aos cinco cabeçalhos por `MainWindow._mostrar_identidade`; a navegação deixou de recalcular. Suíte 2480 (de 2449), 0 falhas; 17 mutações, as 17 reprovam. O par que FICOU PENDENTE naquele dia (o funcionário "Caixa" e o login "Caixa Turno - Noite", id 2, fora de par, com a escrita direta barrada pela permissão da sessão) foi refeito pela tela e está RESOLVIDO: em 2026-09-23 o login id 2 chama-se "Caixa", casa por nome com o funcionário de cargo Caixa (`listar_operadores_caixa` devolve `['Caixa']`), é o único no dropdown e é o `ultimo_operador_id`. Sobra inativo o login id 1 "Caixa Turno - Manhã", sem funcionário de par — invisível em todas as telas, não apagado porque `usuarios.id` é FK de venda. O `.exe` precisa ser gerado de novo. Ainda em 2026-09-21, a pasta do pendrive — §9.29 do REMASTERIZACAO-V1.md: `repository/limpeza_de_vendas.py` + `tools/limpar_vendas.py` apagam só as TABELAS_DE_MOVIMENTO (as mesmas que a semente recusa), zeram saldo de consumo e mesa ocupada, guardam cópia de antes e recusam com o programa aberto; a numeração de caixa e comanda volta ao 1 (nenhuma tabela usa AUTOINCREMENT). Modo portátil em `core/caminhos.py`: no `.exe`, `gestor_comercial.db` na pasta do próprio `.exe` faz dela a pasta de dados; sem ele, `%APPDATA%` como antes. `gerar_exe.py` ganhou `--origem`, monta `App_Pendrive/` (.exe + banco da semente + fotos + LEIA-ME) e prova o modo portátil (cópia da pasta, pasta atual em outro lugar, pasta movida entre aberturas). O banco de trabalho NÃO foi limpo (o programa estava aberto): a semente saiu de uma cópia limpa em `build/banco_limpo/`. Suíte 2497, 0 falhas; 15 mutações, as 15 reprovam; BUILD APROVADO, `App_Pendrive` com 180 arquivos (42,3 MB), 0 venda, 188 produtos, 177 fotos. A impressora de teste 'klm' (ARQUIVO, padrão, caminho em C:\Users\vitor) viaja no banco. PRÓXIMO passo da V1: levar a pasta `App_Pendrive` inteira, copiá-la para o disco da máquina do pai (ex.: C:\GestorComercial), cadastrar a impressora real e rodar docs/checklist-maquina-limpa.md, e depois a validação com ele Em 2026-09-22, o recibo em colunas e a escala da fonte — §9.30 do REMASTERIZACAO-V1.md: services/recibo_formatter.py (cabeçalho com nome/telefone/cidade da loja, tabela CÓDIGO|DESCRIÇÃO|PREÇO|QTD|TOTAL com larguras vindas dos dados e empilhada abaixo de 12 letras de descrição, TOTAL em destaque, OPERADOR(A), LOCAL, PERMANÊNCIA e NÃO É DOCUMENTO FISCAL) usado por recibo e pré-conta; letra_grossa saiu e entrou impressoras.escala_fonte (2x/3x/4x — o 2,5x pedido não existe no GS ! do ESC/POS, decisão do Vitor), aplicada só às linhas de destaque (BlocoTexto.ampliado: nome da loja, mesa, TOTAL); driver manda todo tamanho pelo GS ! e nenhum ESC !; migração d2a8f5c3e917 atômica (escala_fonte DEFAULT 2, DROP letra_grossa, loja_config.nome_loja/telefone/cidade/uf) e seção "Dados da loja" em Configurações. Suíte 2622 passed + 1 corrigido. As três escalas ainda precisam ser testadas na térmica real. Em 2026-09-23 o `.exe` FOI gerado de novo e está em dia com tudo acima: semente saída de uma cópia do banco de trabalho ATUAL (backup API, levando o `-wal`) e limpa com `tools/limpar_vendas.py` (100 linhas de venda apagadas; o banco de trabalho ficou intacto). `dist/GestorComercial.exe` 41,5 MB e `App_Pendrive` com 185 arquivos (42,4 MB), 193 produtos, 182 fotos e 0 venda; provas de fumaça e do modo portátil aprovadas. O build anterior saíra de uma cópia velha em `build/banco_limpo/` e por isso chegou sem as fotos dos 5 produtos de Lanches · Adicionais — desta vez elas estão lá, conferidas arquivo a arquivo dentro da pasta. FALTA na máquina do pai: o banco viaja com a impressora de teste "klm" (ARQUIVO, padrão, caminho em C:\Users\vitor), que precisa virar a térmica real.
local: C:\Vitor Raphael\Códigos\Gestor Comercial Python
repo: https://github.com/VitorRaphael/gestor-comercial-python
---

# O que é
PDV desktop standalone em Python, para a máquina fraca do food truck do pai
do Vitor. **Projeto próprio e independente** — repositório e código-fonte
próprios. Faz o *porte* das regras de negócio, Casos de Uso e Diagrama de
Classes já validados no `GESTOR COMERCIAL` (Java/Spring Boot) para uma stack
Python, mas **não edita nem depende em runtime daquele projeto**. Esse
projeto Java continua existindo separado e intocado.

# Por que existe (não confundir com o Java)
A arquitetura cliente-servidor do Gestor Comercial Java (Spring Boot + PWA em
rede local) está correta como produto, mas exige mais hardware e uma rede
Wi-Fi estável do que a máquina do food truck oferece. Este projeto resolve
isso virando um `.exe` único, standalone, 100% offline, sem servidor.

Uma tentativa anterior (pasta `PVD Python`, repo `pvd-food-truck`) foi
descartada em 2026-08-20 por ter partido de um escopo "inspirado" no Gestor
Comercial em vez de portar o sistema real — a pasta foi então renomeada para
`Gestor Comercial Python` para eliminar essa ambiguidade de vez.

# Regras inegociáveis (RNFs)
- Otimização: leve o suficiente pra máquina fraca do food truck.
- Desempenho: zero travamentos.
- Resiliência offline: nenhuma operação de venda depende de rede.
- Confiabilidade: tratado e testado antes de produção real.

# Escopo V1 (ver docs/arquitetura.md §3 pro detalhe completo)
Autenticação por PIN, Mesas, Comandas, Itens, Pagamentos, Caixa,
Cardápio/Combos, Impressoras, Funcionários, Consumo Interno.
**Fora da V1** (backlog, nesta ordem): App Mobile do Atendente, Controle de
Estoque, Ficha Técnica.

# Stack
PySide6 (LGPL) · SQLAlchemy + Alembic (ORM + migrações) · SQLite · python-escpos
(impressão térmica) · PyInstaller (empacotamento em `.exe` único).

# Modo de trabalho
Write mode — Claude escreve o código diretamente neste projeto (decisão
explícita do Vitor, diferente do teach-mode usado no `ifood-merchant-api`).

# Documentação de referência
- `docs/arquitetura.md` — documento vivo com escopo, regras de negócio,
  árvore de pastas e justificativas de stack.
- `TODO.md` — plano de implementação por fases.
- Diagrama de Classes (V1): https://claude.ai/code/artifact/ef046c14-4e1e-42fb-a27c-8b7c9bf1ddda
